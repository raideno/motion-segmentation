device: cuda:1

run_dir: ???

ckpt: last

dataloader:
  _target_: torch.utils.data.DataLoader
  batch_size: 8
  num_workers: 8

defaults:
  - data: babel-segmentation
  - model: segmentation
  # - trainer
  - defaults
  - _self_

data:
  preload: false